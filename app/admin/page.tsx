import AdminPanel from "../../src/components/admin/AdminPanel";

export const metadata = {
  title: "Admin — Blueportel",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminPanel />;
}
